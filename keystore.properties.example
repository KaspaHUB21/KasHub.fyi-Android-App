# Copy this file to keystore.properties for local Play Store release builds.
# Do not commit keystore.properties or any .jks/.keystore files.
storeFile=release/your-upload-key.jks
storePassword=replace-with-store-password
keyAlias=replace-with-key-alias
keyPassword=replace-with-key-password
